{% macro title(name, no_icon, icon='home') -%}
  {%- if no_icon -%}
    {{ name }}
  {%- else -%}
    <i class="icon-{{ icon }}"></i><span class="icon-label">{{ name }}</span>
  {%- endif -%}
{%- endmacro %}
